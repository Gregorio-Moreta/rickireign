/** Fallback contact inbox when Sanity `siteSettings.contactEmail` is unset
 *  (PLAN open #3). Single source of truth for the form target + legal pages. */
export const FALLBACK_CONTACT_EMAIL = "welcome@rickireign.com";
